enum BoardRole {
    OWNER
    MEMBER
}

enum TaskPriority {
    LOW
    MEDIUM
    HIGH
}

enum ColumnTitle {
    TO_DO @map("To Do")
    IN_PROGRESS @map("In Progress")
    IN_REVIEW @map("In Review")
    DONE @map("Done")
}
